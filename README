type = Global

Joseph Carmody

==Cases
	01) works
	02) works
	03) works
	04) -- ERROR -- Compilation(P4) error 
	05) works
	06) works
	07) -- ERROR -- Parsing error
	08) works
	09) works
	10) -- ERROR -- Infinite loop
	11) works
	12) works

==Notes
	Only prints file.asm or out.asm, depending on if you gave it a file
	Only takes input files with .sp19 after them
	DEBUG mode is run by swithing a bool at the top of any .cpp file

